node_modules/
dist/
*.log

# Local secrets / runtime state — never commit
.env
.env.local
*.sqlite
*.sqlite-*
.sessions.json
.sessions.json.tmp

# Codex auth / session data if it ever lands in-repo by accident
.codex/
auth.json

# Our private dogfooding/operations setup for the live sample host (instance ids, tokens,
# deploy scripts) + our host-specific RUNBOOK. Local-only — never part of the OSS framework.
/dogfooding/

# Intermediate planning artifacts from working sessions (migration/validation plans, etc.).
# Local-only scratch — not part of the OSS framework.
/claude-plans/
